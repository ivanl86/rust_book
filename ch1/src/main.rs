fn main() {
    println!("Hello, world!");
}

/*
cargo new {project_name}
Create a new Rust project directory

cargo init
Create a new Rust project within current directory

Compile using rustc
rustc main.rs
./main

Run the program wihtout executable
cargo run

Compile using Cargo
cargo build

Check the program if it can compile wihout executable
cargo check
*/
